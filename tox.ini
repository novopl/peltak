[tox]
envlist = py27,py34,py36


[testenv]
usedevelop = True
deps = -rrequirements.txt
       -rops/devrequirements.txt
# Just run few commands as smoke tests. Proper tests are needed though
commands = peltak clean
           peltak test --verbose --no-sugar --allow-empty
           peltak docs
           peltak version show
